version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/dev/scripts
    overwrite: yes
file_exists_behavior: OVERWRITE
permissions:
  - object: /home/ubuntu/dev/scripts
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: dev/scripts/stop.sh
      timeout: 60
      runas: root

  ApplicationStart:
    - location: dev/scripts/start.sh
      timeout: 60
      runas: root

