version: 0.0
os: linux

files:
  - source: /
    destination: /home/ubuntu/
    overwrite: yes
file_exists_behavior: OVERWRITE
permissions:
  - object: /home/ubuntu/
    owner: ubuntu
    group: ubuntu

hooks:
  AfterInstall:
    - location: dev/action/scripts/stop.sh
      timeout: 60
      runas: root

  ApplicationStart:
    - location: dev/action/scripts/start.sh
      timeout: 60
      runas: root

